import { SearchfiltPipe } from './searchfilt.pipe';

describe('SearchfiltPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfiltPipe();
    expect(pipe).toBeTruthy();
  });
});
